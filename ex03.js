//### **Usando `push`**
//Crie um array `cores` com as cores `"vermelho"` e `"azul"`. Adicione a cor `"verde"` ao final do array usando `push` e exiba o array atualizado.

const cores = ['vermelho', 'azul'];
cores.push('verde');
console.log(cores);